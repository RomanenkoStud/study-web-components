export * from './autocomplete-combobox';

/*
import {AutocompleteComboboxElement} from './autocomplete-combobox';

const combobox: AutocompleteComboboxElement | null = document.querySelector('#js-controlled-cb');
const options = ['red', 'green', 'blue']

if (combobox) {
    options.forEach((element) => {
        const option = document.createElement('option');
        option.value = element;
        combobox.appendChild(option);
    })

    setTimeout(() => {
        console.log(combobox.options)
    }, 0)
}
*/
